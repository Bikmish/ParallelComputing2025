### 🖥️ Краткое руководство по решателю с использованием OpenMP, MPI и CUDA

[![OpenMP](https://img.shields.io/badge/OpenMP-5.2-blue)](https://www.openmp.org/)
[![MPI](https://img.shields.io/badge/MPI-3.1-orange)](https://www.mpi-forum.org/)
[![CUDA](https://img.shields.io/badge/CUDA-12.0-green)](https://developer.nvidia.com/cuda-toolkit)

---

#### 📚 О проекте

**Автор:** Биктимиров Михаил Геннадьевич, группа 627  
**Курс:** «Суперкомпьютерное моделирование и технологии», МГУ ВМК, 2025

Данный проект — это серия параллельных реализаций численного решателя с использованием современных технологий: **OpenMP**, **MPI**, **CUDA** и их гибридных комбинаций.  
В репозитории представлены исходные коды, инструкции по сборке и запуску, а также рекомендации по тестированию и сбору статистики для отчёта.

---

#### 📂 Структура проекта

Проект состоит из четырёх папок, каждая из которых содержит реализацию для определённой технологии параллельных вычислений:

- **task1_OMP** — многопоточная реализация на OpenMP
- **task2_MPI** — распределённая реализация с передачей сообщений (MPI)
- **task3_MPI_OMP** — гибридная реализация (MPI + OpenMP)
- **task4_MPI_CUDA** — гибридная реализация (CUDA + MPI, вычисления на GPU)

В каждой папке есть свой `Makefile` с индивидуальными командами компиляции и запуска.

---

#### ⚙️ Компиляция и запуск

##### OpenMP

- **Компиляция:**
    - Локально:  
      `clang++ -O3 -std=c++11 -fopenmp solver.cpp main.cpp -o solver`  
      или через Makefile:  
      `make build`
    - На Polus:  
      `g++ -O3 -std=c++11 -fopenmp solver.cpp main.cpp -o solver`  
      или через Makefile:  
      `make build_polus`
- **Тестирование и сбор статистики:**
    - `make test_n256_l1_thAll` — N=256, L=1, потоки: 1,2,4,8,16,32
    - `make test_n256_lPi_thAll` — N=256, L=Pi, потоки: 1,2,4,8,16,32
    - `make test_full_config` — N:128,256,512; L:1,Pi; потоки:1,2,4,8,16,32
    - Аналогичные команды с суффиксом `_polus` для запуска на Polus

> Для полноты отчёта и корректности выводов для всех версий были проведены всевозможные запуски с разными конфигурациями параметров и разной привязкой, с последующим усреднением статистики.

---

##### MPI

- **Компиляция:**
    - `mpixlC -O3 -std=c++11 -o solverMPI_only main.cpp`
    - или через Makefile:  
      `make build` или просто `make`
- **Тестирование и сбор статистики:**
    - `make run_all` — N:128,256,512; L:1,Pi; процессы:1,2,4,8,16,32
    - `make clean` — очистка результатов
    - `make help` — справка по командам

---

##### MPI + OpenMP

- **Компиляция:**
    - `mpixlC -O3 -qsmp=omp -std=c++11 -o solverMPI main.cpp`
    - или через Makefile:  
      `make build` или просто `make`
- **Тестирование и сбор статистики:**
    - `make run_all` — N:128,256,512; L:1,Pi; MPI:4,8 проц; OMP:1,2,4,8 потоков
    - `make run_all_1` — N:128,256,512; L:1,Pi; MPI:1,4,8,16,32 проц; OMP:1 поток
    - `make run_all_max` — N:128,256,512; L:1,Pi; MPI:8 проц; OMP:20 потоков
    - `make clean` — очистка результатов
    - `make help` — справка по командам

---

##### CUDA + MPI

- **Компиляция:**
    - `nvcc -O3 -arch=sm_35 -ccbin mpic++ -o solverCUDA main.cu -x cu -Xcompiler -Wall -std=c++11`
    - или через Makefile:  
      `make build`
- **Тестирование и сбор статистики:**
    - `make run_test` — N=256, L=Pi, MPI:8 проц, 1 GPU shared (тестовый запуск)
    - `make run_all_single_gpu` — N:128,256,512; L=Pi; MPI:1,2,4,8,16 проц; 1 GPU shared
    - `make run_all_single_gpu_ex` — N:128,256,512; L=Pi; MPI:1,2,4,8,16 проц; 1 GPU exclusive
    - `make run_all_double_gpu` — N:128,256,512; L=Pi; MPI:1,2,4,8,16 проц; 2 GPU shared
    - `make run_all_double_gpu_ex` — N:128,256,512; L=Pi; MPI:1,2,4,8,16 проц; 2 GPU exclusive
    - `make clean` — очистка

> Для CUDA-реализации основное сравнение проводилось между конфигурациями «1 ГПУ + 1 процесс» и «2 ГПУ + 2 процесса», остальные варианты запускались для полноты эксперимента.

---

#### 📊 Отчёт и анализ

Подробный анализ статистики, выводы, таблицы (1–15), графики (1–12) и детали реализации каждой версии программ приведены в приложенном к архиву отчёте.

---

#### 📎 Ссылки

- [GitHub-репозиторий с исходным кодом всех версий](https://github.com/Bikmish/ParallelComputing2025)

---

**Надеюсь, это кому-нибудь поможет :) Удачи!** 🚀
